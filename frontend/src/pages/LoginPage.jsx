import { useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import api, { setAccessToken } from "../api/api";
export default function LoginPage() { const [login,setLogin]=useState(""); const [password,setPassword]=useState(""); const [error,setError]=useState(""); const [loading,setLoading]=useState(false); const navigate=useNavigate(); async function submit(e){e.preventDefault();setLoading(true);setError("");try{const {data}=await api.post("/auth/login",{login,password});setAccessToken(data.data.access_token);navigate("/");}catch(err){setError(err.response?.data?.error?.message||"Не удалось войти");}finally{setLoading(false);}} return <main className="auth-page"><form className="auth-card" onSubmit={submit}><span className="eyebrow">HOTEL OPS</span><h1>С возвращением</h1><p>Войдите, чтобы управлять инцидентами.</p>{error&&<div className="form-error">{error}</div>}<label>Логин или email<input value={login} onChange={e=>setLogin(e.target.value)} required /></label><label>Пароль<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required /></label><button disabled={loading}>{loading?"Вход…":"Войти"}</button><small>Нет аккаунта? <Link to="/register">Зарегистрироваться</Link></small></form></main>; }
