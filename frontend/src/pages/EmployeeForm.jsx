import { useEffect, useState } from "react";
import { useNavigate, useParams } from "react-router-dom";
import api from "../api/api";

export default function EmployeeForm(){const {id}=useParams();const isEdit=Boolean(id);const navigate=useNavigate();const [form,setForm]=useState({full_name:"",position:"",hotel_id:""});const [hotels,setHotels]=useState([]);const [error,setError]=useState("");const [saving,setSaving]=useState(false);useEffect(()=>{Promise.all([api.get("/hotels"),isEdit?api.get(`/employees/${id}`):Promise.resolve(null)]).then(([h,e])=>{setHotels(h.data.data||[]);if(e)setForm(e.data.data)}).catch(()=>setError("Не удалось загрузить форму"));},[id,isEdit]);const change=e=>setForm({...form,[e.target.name]:e.target.value});async function submit(e){e.preventDefault();setSaving(true);setError("");try{if(isEdit)await api.put(`/employees/${id}`,form);else await api.post("/employees",form);navigate("/employees");}catch(err){setError(err.response?.data?.error?.message||"Не удалось сохранить сотрудника");}finally{setSaving(false);}}return <div className="form-card"><h1>{isEdit?"Редактировать сотрудника":"Новый сотрудник"}</h1>{error&&<p className="form-error">{error}</p>}<form onSubmit={submit}><input name="full_name" placeholder="ФИО" value={form.full_name} onChange={change} required/><input name="position" placeholder="Должность" value={form.position} onChange={change} required/><select name="hotel_id" value={form.hotel_id} onChange={change} required><option value="">Выберите гостиницу</option>{hotels.map(h=><option key={h.id} value={h.id}>{h.name}</option>)}</select><button disabled={saving}>{saving?"Сохраняем…":"Сохранить"}</button></form></div>}
